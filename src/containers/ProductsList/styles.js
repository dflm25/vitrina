/**
 * 
 */
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 22,
    textAlign: 'left',
    fontWeight: 'bold',
    marginBottom: 10,
  },
  contentIndicator: {
    marginBottom: 20,
  }
});

export default styles;